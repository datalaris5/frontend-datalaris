# Netlify Configuration
# =====================
# Konfigurasi untuk deploy ke Netlify

[build]
  command = "npm run build"
  publish = "dist"

# SPA Redirect - Semua route diarahkan ke index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
